#!/bin/bash
# Thin, memorable alias for submit_calibration_job.sh (see that script for the
# full T1/T2/T3 flow: NORC-ranking counter selection -> measurement job ->
# automated Extra-P modeling-quality analysis). Kept as a wrapper rather than
# a copy so there's exactly one implementation to maintain.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/submit_calibration_job.sh" "$@"
