# Weak scaling: each rank owns a fixed-size sub-domain, so the global size grows
# with the number of ranks along one axis.
weak_x -nx $((20 * ranks)) -ny 20 -nz 20
weak_x_scaled -nx $((20 * ranks)) -ny $((20 * threads)) -nz 20
weak_z -nx 10 -ny 10 -nz $((10 * ranks))
